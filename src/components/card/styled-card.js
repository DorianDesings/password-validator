import styled from 'styled-components';

const StyledTitle = styled.h1`
	text-align: center;
`;

const StyledCard = styled.div`
	text-align: center;
	width: 350px;
	height: 400px;
	margin-inline: auto;
	padding: 0.5rem 1rem;
	background-color: #111;
	border-radius: 1rem;
`;

export { StyledTitle, StyledCard };
