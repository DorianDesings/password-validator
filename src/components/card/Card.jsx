import Form from '../form/form';
import { StyledCard, StyledTitle } from './styled-card';

const Card = () => {
	return (
		<StyledCard>
			<StyledTitle>Password Validator</StyledTitle>
			<Form />
		</StyledCard>
	);
};

export default Card;
